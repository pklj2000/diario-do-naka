/// <reference types="node" />
import { EventEmitter } from 'events';
export interface FunctionServerOptions {
    port?: number;
}
export declare class FunctionServer extends EventEmitter {
    opts: FunctionServerOptions;
    port?: number;
    host?: string;
    constructor(opts?: FunctionServerOptions);
    run(): Promise<void>;
}
